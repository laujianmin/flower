# INSTALLMENT
```
python 3.10.16
打开到flwr-diy环境来覆盖安装
pip install .
pip install ".[simulation]"
python -m pip install "flwr-datasets[vision]"
------
pip uninstall flwr
pip show flwr
pip uninstall ...
```



# Git

```
git status
git add . 
git commit -S -m "message"
git push
# 失败的话，检查虚拟环境是否为flwr-diy
```